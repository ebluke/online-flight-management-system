const handleClick = () => {
  console.log("Button work");
};

function App() {
  return (
    <div>
      <button onClick={() => handleClick()}>Testing text</button>
      <text></text>
    </div>
  );
}

export default App;
